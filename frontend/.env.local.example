# Copy this to frontend/.env.local and fill in your keys. Never commit .env.local.

# Sentry browser SDK — create a separate project in Sentry for the frontend
# Settings → Projects → Add Project (Browser JavaScript / React)
VITE_SENTRY_DSN=https://your-key@o0.ingest.sentry.io/0
